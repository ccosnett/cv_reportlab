[project]
name = "cv-reportlab"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
  { name = "ccosnett", email = "conorcosnett@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
  "pip>=24.2",
  "ipykernel>=6.29.5",
  "reportlab==4.1.0",
  "pyyaml==6.0.1"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

