[project]
name = "cv-reportlab"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "ccosnett", email = "conorcosnett@gmail.com" }]
dependencies = [
  "ipykernel>=6.29.5,<7",
  "reportlab==4.1.0",
  "PyYAML==6.0.1"
]

[tool.setuptools]
py-modules = ["main", "utils"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
